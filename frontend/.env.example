# Vite proxies /api locally; Vercel forwards it to BACKEND_URL in production.
VITE_API_BASE_URL=/api/v1
# Set only in Vercel to the deployed Render origin, without /api/v1.
# BACKEND_URL=https://your-render-service.onrender.com
